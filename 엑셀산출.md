# 파이프라인: 엑셀산출

시트 반영과 별개로, **어디가 원본을 대학별 입결 엑셀로 산출**하는 파이프라인.
수시/정시 분리(헤더 상이), 미제출 사유 행 제거.

## 1) 크롤 (브라우저 JS — 로그인 세션 사용, 클릭·렌더링 없음)
데이터 XHR을 직접 호출한다. `get_page_text`보다 빠르고 대량에 적합.
- 엔드포인트: `POST /uct/acd/ade/criteriaAndResultItemNewAjax.do`
- 헤더: `Content-Type: application/x-www-form-urlencoded;charset=UTF-8`, `X-Requested-With: XMLHttpRequest`,
  `X-CSRF-TOKEN`(페이지 `meta[name=_csrf]`에서 읽음). 쿠키는 브라우저 세션이 자동 첨부(값 안 만짐).
- 본문: `searchSyr=2027 & unvCd=<코드> & tsrdCmphSlcnArtclUpCd=<20|30|40> & compUnvCd=`
  - **전형유형 코드: 20=학생부종합 · 30=학생부교과 · 40=수능위주(정시)**
- 응답은 **HTML 표**(JSON 아님) → `<table>`의 `<tbody tr>`를 셀 평탄화. 정시=31칸(구분+모집단위+29), 수시=11칸.
  값은 `99.0`/`0.0`로 옴 → `.0` 제거, `0`·`-`·빈칸은 빈값 처리.
- **미제출·최초(A)=0 행은 크롤 단계에서 제거**(셀에 '미제출' 포함 or 최초 비었으면 skip).
- 수시(20+30)·정시(40)를 **한 파일**에 `===JEONGSI===` 마커로 이어붙여 **1회 다운로드**
  (크롬은 페이지당 2번째 자동 다운로드를 막으므로 반드시 1파일). → `Downloads/adiga_<대학>_raw.tsv`

정시 백분위 컬럼 순서: 국어·수학·탐구1(사/과/직)·탐구2(사/과/직)·평균백분위·한국사(등급)·영어(등급) = 11칸, 50%·70% 각각.

## 2) 엑셀 산출 (파이썬)
```
py adiga_xlsx.py <raw.tsv> [출력폴더]     # 마커로 쪼개 수시/정시 xlsx 2개, 숫자컬럼 숫자화
py adiga_xlsx.py --selfcheck
```
- 출력: `<대학>_수시_2026.xlsx` / `<대학>_정시_2026.xlsx`. 대상 파일이 엑셀에 열려 잠기면 `_new` 붙여 저장.
- 수시/정시는 **헤더가 달라 파일을 분리**(수시=등급, 정시=백분위 국어~영어 세부).

## 로그인·보안
- 로그인은 **자동화 안 함**(자격증명 입력 금지) — 사용자가 직접. 세션 만료 시 1회 재로그인하면 XHR 지속.
- CSRF·쿠키는 브라우저 세션에만 존재, 스크립트/로그에 저장 안 함.

## 전체 확장 시
- **4년제 대학 목록(220개): `대학목록_4년제.tsv`** (컬럼 `unvCd`+`대학명`) — 크롤 루프의 입력 소스.
  (추출: `univView.do` 일반대학 탭에서 `label[for^="UNI_"]`의 for=`UNI_<unvCd>`+라벨텍스트. 전문대는 `collView.do`.)
  캠퍼스는 unvCd·입결이 별개라 각각 크롤 대상(본교 190 + 제2캠퍼스·분교 30).
- 대학별 fetch·평탄화·미제출제거를 누적 → **전체를 1파일로 모아 1회 다운로드**(다중 다운로드 차단 회피) → `adiga_xlsx`.
- 대학당 3요청(~수 초) → 352개 약 40분~1시간. 세션 만료 시 재로그인 후 이어서.
