# 파이프라인: 시트반영

외부/어디가 자료(와이드포맷)를 시트 롱포맷(`지표`+`척도`+`값`)으로 녹여 구글시트(`01_fact_admissions`)에
**매칭·검산 후 추가**하는 작업의 기준. 반영 작업 전 이 문서를 참고한다.

## 대원칙: 기존 시트 입력 규칙에 맞는 정보만 넣는다

- 신규 `지표`·`척도`는 만들지 않는다. 기존에 없는 값(총지원·합격인원·실경쟁률·만점 등)은 제외.
- 값이 `0`·빈칸인 행은 넣지 않는다(기존 시트에 충원=0 행이 하나도 없는 것과 동일한 규칙).
- 자료에만 있고 시트에 없는 컬럼(지역 등)은 버린다.

## 매칭 원칙: 정체는 학과명이 아니라 값(모집인원·경쟁률)으로 확정한다

자료의 학과명·세부전형명이 시트와 **정확히 일치하지 않을 수 있다**(표기 차이·라벨 오류·전형별 명칭 상이). 이름이 아니라 **모집인원(+경쟁률) 일치로 시트 행을 특정**하고, 자료 라벨을 맹신하지 않는다.

- 매칭 키(1차): 대학명(정규화)·연도·모집기간·계열·모집단위, `전형`으로 세부전형 특정.
- 이름이 안 맞으면 같은 대학·연도·모집기간 안에서 **모집인원·경쟁률이 일치하는 행**으로 확정.
  - 예: 자료 "천문학전공" 3행 → 값 대조로 2행은 실제 `물리·천문학부 물리학전공`, 1행만 `천문학전공`.
- 표기 정규화: 가운뎃점 `・`(U+30FB) → `·`(U+00B7) 등 문자 차이를 맞춘다.
- **학과명·세부전형이 헷갈리면 반드시 사용자에게 확인**하고 진행(임의 매칭 금지).
- **값 엇갈림(swap) 감지**: 두 항목의 모집인원·경쟁률이 서로 **교차 일치**하면 자료 입력 오류를 의심.
  학과 라벨·계열의 내부 일관성으로 판정하되, 애매하면 보류하고 질문한다.
  (예: 자료 자유전공/치의 모집인원이 뒤바뀌어 입력된 사례 → 학과 라벨 기준으로 정정 반영)

## 식별컬럼은 기존 행과 조인해 가져온다

자료엔 없는 식별컬럼(대학선호도·단과대·학과번호·전형번호·정원내외)은 확정된 시트 행에서 조인해 채운다.

- 대학명 정규화: `서울대` → `서울대학교(서울)` 형태(기존 표기에 맞춤).
- 매칭 실패 행은 반영하지 말고 목록으로 보고.

## 검산(반영 전 필수)

- 자료의 모집인원·경쟁률이 매칭된 기존 행 값과 **일치**해야 반영한다.
- 불일치 행은 보류하고 원인과 함께 보고. (매칭 오류거나 데이터 변경)
- 단, **경쟁률이 완전 일치**하면 동일 전형으로 보고 반영하되, 모집인원 소폭차(±1 등)·반올림차는 **기록·보고**한다. 기존 값은 자료로 덮지 않는다.
- **확정 오타는 정정**: 시트값이 명백한 오류(자릿수 뒤바뀜 등)이고 사용자가 자료값이 맞다고 확인해 준 경우에 한해, `values.update`로 해당 셀을 정정한다. (앞줄의 '덮지 않는다'는 애매한 소폭차에 대한 규칙 — 확인된 오타에는 적용하지 않음.)
- 수치 집계는 CLAUDE.md의 검산 지침(독립 2경로)을 따른다.
- **반영 후 `py ipsi.py --refresh`로 재조회**해, 생성한 값이 시트에 그대로 들어갔는지 대조(스팟체크 포함)한다.

## 수시 척도 매핑(확정)

| 자료 컬럼 | 지표 | 척도 |
|---|---|---|
| 등급50 | 합격선 | 내신_50% |
| 등급70 | 합격선 | 내신_70% |
| 추합 | 충원 | (빈) — 값>0만 |

- 모집인원·경쟁률: 이미 존재 → 추가 안 함(검산용으로만 사용).
- 변환50/70: 수시엔 해당 척도 없음 → 제외.
- 자료 한 행 → 시트에 최대 3행(내신_50%, 내신_70%, 충원) 추가.

## 정시 척도 매핑(확정)

| 자료 컬럼 | 지표 | 척도 |
|---|---|---|
| 변환50 | 합격선 | 수능_대학제공점수_50% |
| 변환70 | 합격선 | 수능_대학제공점수_70% |
| 평균70 | 합격선 | 수능_백분위_70% |
| 추합 | 충원 | (빈) — 값>0만 |

- 모집인원·경쟁률: 이미 존재 → 추가 안 함(검산용으로만 사용).
- **평균50 제외**: 시트에 `수능_백분위_50%` 척도가 없음(백분위는 70%만, 대학제공점수는 50/70 둘 다). 신규 척도 안 만드는 규칙대로 버린다.
- **제외 컬럼**: 지역, 만점, 국어·수학·탐구·평균·영어(영역별 점수), 총지원지원, 합격인원, 실경쟁률, 확통·미적·기하(수학 선택과목) — 대응 지표·척도 없음.
- 값 포맷은 기존 관례대로 불필요한 0 제거(406.10→406.1, 97.00→97).
- 자료 한 행 → 시트에 최대 4행(대학제공_50%, 대학제공_70%, 백분위_70%, 충원) 추가.

### 정시 식별컬럼 조인
- 자료 `대학`→`대학명`(정규화: 서울대→서울대학교(서울)), `연도`→`학년도`, `수시/정시`→`모집기간`(정시),
  `인문/자연`→`계열`, `학과`→`모집단위`, `전형`→`세부전형`.
- 자료 `전형`의 군 표기(예 `정시(나)`)는 시트 `지원군`(나)에 대응. `전형유형`은 대부분 `수능위주` — 조인으로 채움.
- 라벨이 시트와 안 맞을 수 있음(예 자료 `(학부대학)광역`/`통합`/`정시(나)` ↔ 시트 `광역`/`인문자연`/`일반전형`).
  → 이름 맹신 말고 **모집인원+경쟁률 동시 일치**로 행 특정(둘이 겹치는 후보 여럿이면 보류·질문).

## 어디가(adiga) 크롤 → 시트반영 (수시+정시)

수기 엑셀 대신 **어디가에서 직접 크롤**해 반영한다(전사오타·라벨오류 원천 제거).
어디가 결과표는 시트에 필요한 값을 다 갖고 있고, **모집인원은 최초(A)가 시트값과 일치**(시트는 최초 기준).
(대량 엑셀 산출은 별도 파이프라인 `엑셀산출.md`의 XHR 방식 사용.)
- **정시(Ⅳ.수능위주)**: 환산점수50/70(=변환) · 평균백분위50/70(=평균) · 최초/이월/최종 · 경쟁률 · 충원. (데이터 29칸)
- **수시(Ⅱ.학생부종합·Ⅲ.학생부교과)**: 환산점수50/70 · **환산등급50/70(=내신)** · 최초/이월/최종 · 경쟁률 · 충원. (데이터 9칸)
  구분열이 `수시`(지원군 없음=시트 지원군 `''`), 종합=`학생부위주(종합)`·교과=`학생부위주(교과)`.

### 1) 크롤 (브라우저, 로그인 세션 필요 — 로그인은 사용자가 직접)
- 검색으로 대학 `unvCd` 확인 (예 서울대=`0000019`). 검색결과 링크 href의 `unvCd=` 값.
- URL: `https://www.adiga.kr/ucp/uvt/uni/univDetailSelection.do?menuId=PCUVTINF2000&unvCd=<코드>&searchSyr=2027`
  (searchSyr=2027 페이지 안에 **2026 결과**가 들어 있음)
- **탭마다** 반복: Ⅱ.학생부종합 → Ⅲ.학생부교과 → Ⅳ.수능위주. 각 탭에서 아코디언 **"2026학년도 전형 결과"** 펼침(AJAX)
  → `get_page_text` → **한 덤프.txt에 이어붙임**(파서가 섹션 헤더로 수시/정시·전형유형·세부전형을 자동 구분. 헤더/푸터 중복은 무시됨).
- (참고) 내부 XHR: `POST /uct/acd/ade/criteriaAndResultItemNewAjax.do`. 시트반영엔 get_page_text로 충분.

### 2) 파싱·반영 (파이썬)
```
py adiga_parse.py <덤프.txt> <대학명>            # 표 → 와이드행(JSON), 미제출·최초0 제외
py adiga_reflect.py <덤프.txt> <대학명>          # dry-run: 매칭·검산·생성행 리포트
py adiga_reflect.py <덤프.txt> <대학명> --commit # 실제 append
py adiga_parse.py --selfcheck                    # 파서 회귀 점검(수시+정시)
```
- 매핑: 정시=환산50/70→대학제공50/70·평균70→백분위70·충원 / 수시=등급50/70→내신_50/70·충원.
- 매칭: (모집기간·지원군·모집단위정규화·세부전형)로 **후보 리스트**를 잡고 **최초(A)로 특정**(이름 안 맞으면 값으로 구제). 검증은 경쟁률 일치.
  - 같은 모집단위명이 **인문/자연 양 계열에 존재**해도(예 진리자유학부) 최초(A)로 정확히 가름 — 키에 계열 안 넣음(어디가가 계열 미제공).
- 이미 있는 척도는 skip(중복 방지), 검산은 독립 2경로 자동. **검산 불일치면 반영 중단**.
- 세부전형명이 시트와 그대로 일치(일반전형/지역균형전형/기회균형특별전형_*). 크롤이 **시트 빈칸(미입력 세부전형)도 자동으로 메움**.

### 경계 (자동화 한계 — 보류·수동 처리)
- 반영기는 **기존 모집단위/세부전형 행에 합격선·충원만 얹는다.** 시트에 없는 신규 모집단위·전형(예 일부 대학의 기회균형)은 보류로 보고 → 식별컬럼(단과대·학과번호·전형번호·정원내외)은 어디가에 없으므로 같은 모집단위 일반전형에서 조인해 **수동 생성**(저소득=전형번호7·정원외, 농어촌=6·정원외).
- 경쟁률 불일치·미공개(경쟁률0) 행은 보류. 확정 오타는 사용자 확인 후 정정.

## 참고: 시트 컬럼(16개)

대학선호도 · 모집기간 · 지원군 · 대학명 · 계열 · 단과대 · 학과번호 · 모집단위 ·
전형유형 · 전형번호 · 세부전형 · 정원내외 · 학년도 · 지표 · 척도 · 값
