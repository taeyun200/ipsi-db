# ipsi-db 작업 지침

개인 진학지도 DB(대입). 구글 시트를 불러와 조건별로 정리·집계한다.

## 필수 지침: 계산 결과는 반드시 검산

수치(합계·집계·통계)를 출력할 때는 **반드시 검산하고, 검산 결과를 함께 밝힌다.**

- 집계는 **서로 다른 독립 경로로 최소 2번** 계산해 일치를 확인한다.
  (예: 세부전형/지원군 소계의 합 vs 전체 행 독립 총합)
- 정수 변환이 안 되는 값(빈 값·오타)은 개수를 확인하고 보고한다.
- 검산이 어긋나면 결과를 내보내지 말고 원인부터 규명한다.
- 출력 시 "검산 통과"와 대조한 두 값이 일치함을 짧게 명시한다.

## 도구

`ipsi.py` — 시트(`01_fact_admissions`)를 로컬 캐시 후 조건으로 걸러 정리 출력.

```
py ipsi.py                              # 조회 가능한 컬럼·값 목록
py ipsi.py 대학명=서울대 학년도=2028 지표=모집인원
py ipsi.py --refresh 대학명=고려대       # 시트 새로고침 후 조회
```

필터: 여러 개 AND, 값은 부분일치(대소문자 무시).
데이터는 롱포맷(지표+척도+`값`) — 인원 집계는 `값` 컬럼을 정수 합산한다.

## 작업 파이프라인

작업 규칙은 파이프라인별 파일로 분리 관리한다(토큰 절약 — **해당 작업 때 그 파일만 Read**).
CLAUDE.md는 이름·포인터만 유지한다.

- **시트반영** → `시트반영.md`
  외부/어디가 자료를 매칭·검산해 구글시트(`01_fact_admissions`)에 반영.
  척도매핑(수시=내신·정시=대학제공/백분위)·값매칭·독립2경로 검산·`adiga_parse`/`adiga_reflect`.
- **엑셀산출** → `엑셀산출.md`
  어디가 XHR 크롤로 대학별 입결을 **수시/정시 엑셀**로 산출. 미제출 행 제거·`adiga_xlsx`.
- **평가기준DB** → `평가기준DB.md`
  어디가 '전형별 주요사항'(종합·교과·수능)을 대학×전형유형×섹션으로 쪼개 SQLite 적재. `adiga_criteria_db.py`.
  시트에는 넣지 않음(서술·표라 지표/척도/값 규칙에 안 맞음).
- **인원조회출력** → 스킬 `ipsi-report` (`.claude/skills/ipsi-report/`)
  수시/정시 모집인원을 전형유형·세부전형·지원군별로 집계·검산해 표로 출력. `ipsi_report.py` 사용.

공통 최소 기준: 기존 시트 입력 규칙에 맞는 정보만(신규 지표·척도·0값 제외), 식별컬럼은 기존 행 조인, 모집인원·경쟁률로 검산 후 반영.

## 접근

- Google Sheets API + 서비스 계정. `credentials.json`(서비스 계정 키)은 커밋 금지(gitignore).
- credentials.json은 구글 드라이브 클로드 폴더에 넣어놨음. 확인할 것
- 대상 시트는 서비스 계정(`mcp-sheets@used-make.iam.gserviceaccount.com`)에 **편집자로 공유**돼 있음
  → 읽기(`ipsi.py`)뿐 아니라 쓰기 가능. 반영은 write 스코프 + `values.append`(추가) / `values.batchUpdate`(개명 등 수정).
- 콘솔 한글 깨짐 방지: `PYTHONUTF8=1` 로 실행.
